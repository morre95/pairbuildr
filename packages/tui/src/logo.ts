// The PairBuildr wordmark, split into its parts so each can be colored by role.
//
//   ┌─ PairBuildr
//   └─ build alongside
export const LINES = {
  rule: "┌─ ",
  name: "PairBuildr",
  corner: "└─ ",
  tagline: "build alongside",
} as const
